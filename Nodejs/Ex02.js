//This will consume the table.js inside  this js file and consume it. 
const mathTable = require('./table')//Use ./ for the local folder and is used for local modules. 

mathTable(5);
mathTable();
